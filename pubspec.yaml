name: flutter_bloc
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.5

flutter:
  uses-material-design: true
